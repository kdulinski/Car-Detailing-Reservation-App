import styles from "./Slider.module.css";

const Slider = () => {
  return (
    <div className={styles.slider}>
      <div className={styles.slogan}>Best detailing ever</div>
    </div>
  );
};

export default Slider;
